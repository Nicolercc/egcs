---
const currentYear = new Date().getFullYear();

const services = [
    { name: "Project Management", nameEs: "Gestión de Proyectos", url: "/services/project-management" },
    { name: "Asbestos Management", nameEs: "Gestión de Asbesto", url: "/services/asbestos-management" },
    { name: "Cleaning Services", nameEs: "Servicios de Limpieza", url: "/services/cleaning" },
];

const companyLinks = [
    { name: "About Us", nameEs: "Sobre Nosotros", url: "/about" },
    { name: "Contact", nameEs: "Contacto", url: "/quote" },
    { name: "Get a Quote", nameEs: "Obtener Presupuesto", url: "/quote" },
];

const socialLinks = [
    { name: "Facebook", url: "#", icon: "fab fa-facebook-f" },
    { name: "Twitter", url: "#", icon: "fab fa-twitter" },
    { name: "LinkedIn", url: "#", icon: "fab fa-linkedin-in" },
];
---

<footer class="bg-gray-800 text-white pt-12 pb-8">
    <div class="container mx-auto px-4">
        <div class="grid grid-cols-1 md:grid-cols-4 gap-8">
            <div class="mb-8 md:mb-0">
                <h3 class="text-xl font-bold mb-4">EGCS</h3>
                <p class="text-gray-400 lang-en">Providing expert project management and services to empower your space.</p>
                <p class="text-gray-400 lang-es hidden">Brindamos gestión de proyectos y servicios expertos para potenciar su negocio.</p>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 lang-en">Services</h4>
                <h4 class="text-lg font-semibold mb-4 lang-es hidden">Servicios</h4>
                <ul class="space-y-2">
                    {services.map(service => (
                        <li>
                            <a href={service.url} class="text-gray-400 hover:text-white transition-colors">
                                <span class="lang-en">{service.name}</span>
                                <span class="lang-es hidden">{service.nameEs}</span>
                            </a>
                        </li>
                    ))}
                </ul>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 lang-en">Company</h4>
                <h4 class="text-lg font-semibold mb-4 lang-es hidden">Empresa</h4>
                <ul class="space-y-2">
                    {companyLinks.map(link => (
                        <li>
                            <a href={link.url} class="text-gray-400 hover:text-white transition-colors">
                                <span class="lang-en">{link.name}</span>
                                <span class="lang-es hidden">{link.nameEs}</span>
                            </a>
                        </li>
                    ))}
                </ul>
            </div>
            <div>
                <h4 class="text-lg font-semibold mb-4 lang-en">Connect With Us</h4>
                <h4 class="text-lg font-semibold mb-4 lang-es hidden">Conéctate con Nosotros</h4>
                <div class="flex space-x-4">
                    {socialLinks.map(link => (
                        <a href={link.url} class="text-gray-400 hover:text-white transition-colors" aria-label={link.name}>
                            <i class={link.icon}></i>
                        </a>
                    ))}
                </div>
            </div>
        </div>
     
    </div>
</footer>

